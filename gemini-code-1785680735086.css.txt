/* ==========================================================================
   ZeqaValues - Popup System Stylesheet
   Dark theme, backdrop blur, mint accent highlights, entrance/exit animations
   ========================================================================== */

/* Dark Overlay with Blur */
.zv-popup-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  background: rgba(10, 10, 10, 0.75);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 9999;
  opacity: 0;
  transition: opacity 0.25s ease-in-out;
}

.zv-popup-overlay.active {
  opacity: 1;
}

/* Modal Window Container */
.zv-popup-container {
  background: #191919;
  border: 1px solid #2a2a2a;
  border-radius: 16px;
  width: 92%;
  max-width: 860px;
  max-height: 90vh;
  overflow-y: auto;
  box-shadow: 0 20px 50px rgba(0, 0, 0, 0.8), 0 0 20px rgba(94, 242, 182, 0.05);
  color: #ffffff;
  transform: scale(0.95) translateY(10px);
  transition: transform 0.25s cubic-bezier(0.16, 1, 0.3, 1);
  padding: 28px;
  box-sizing: border-box;
}

.zv-popup-overlay.active .zv-popup-container {
  transform: scale(1) translateY(0);
}

/* Scrollbar Customization */
.zv-popup-container::-webkit-scrollbar {
  width: 6px;
}
.zv-popup-container::-webkit-scrollbar-track {
  background: #111111;
  border-radius: 8px;
}
.zv-popup-container::-webkit-scrollbar-thumb {
  background: #333333;
  border-radius: 8px;
}
.zv-popup-container::-webkit-scrollbar-thumb:hover {
  background: #5EF2B6;
}

/* Modal Header */
.zv-popup-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-bottom: 1px solid #282828;
  padding-bottom: 18px;
  margin-bottom: 20px;
}

.zv-popup-title-wrap {
  display: flex;
  align-items: center;
  gap: 12px;
}

.zv-popup-title {
  margin: 0;
  font-size: 24px;
  font-weight: 700;
  letter-spacing: -0.5px;
  color: #ffffff;
}

.zv-rarity-badge {
  font-size: 11px;
  font-weight: 700;
  text-transform: uppercase;
  padding: 4px 10px;
  border-radius: 6px;
  background: #252525;
  color: #5EF2B6;
  border: 1px solid rgba(94, 242, 182, 0.3);
  letter-spacing: 0.5px;
}

.zv-popup-close-btn {
  background: #222222;
  border: 1px solid #333333;
  color: #aaaaaa;
  width: 36px;
  height: 36px;
  border-radius: 50%;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 16px;
  transition: all 0.2